%h4
  You've got total
  = @new_episodes_size
  new episodes🎉, here are the some of them!

%p
  Click
  = link_to 'here', porch_url
  to check out all of your digest 👀😊

- @random_episodes.each do |item|
  %hr
  = item.title.html_safe
  = "by '#{item.feed.name}' @ #{item.published_at.to_formatted_s(:short)}"
  %br
  = link_to "listen", player_url(slug: item.slug), target: 'episode_player'
%hr

= render 'partials/mail_footer'

