<section class="section">
    <div class="container">
        <h2 class="title is-2">
            likes
            💗
        </h2>
        <hr/>
    </div>
    <div class="container">
        <% if @items.present? %>
            <% @items.each do |item| %>
                <%= render partial: 'partials/episode', locals: { item: item.episode, show_dismiss_button: false, show_image: true, show_short_date: false } %>
            <% end %>
        <% else %>
            You haven't liked any episodes yet 💔
            <br/>
        <% end %>
    </div>
    <div class="container">
        <%= render 'partials/page_navigation' %>
    </div>
</section>
