<section class="section">
    <div class="container">
        <h2 class="title is-2">
            listen-it-later
            📼
        </h2>
        <hr/>
    </div>
    <div class="container">
        <% if @items.present? %>
            <% @items.each do |item| %>
                <%= render partial: 'partials/episode', locals: { item: item, show_dismiss_button: false, show_images: true } %>
            <% end %>
        <% else %>
            You don't have any episodes added to listen-it-later.
            <br/>
        <% end %>
    </div>
    <div class="container">
        <%= render 'partials/page_navigation' %>
    </div>
</section>
